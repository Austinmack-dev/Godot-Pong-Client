extends Node2D

# Declare member variables here. Examples:
# var a = 2
# var b = "text"

# Called when the node enters the scene tree for the first time.
func _ready():
	pass
#	get_node("PlayerName").text = "Player Name is: " + pName
#	get_node("PlayerNumber").text = "Player Number is: " + str(pNumber)
#	get_node("ReadyButton").visible = pReadyButton
#	get_node("PlayerReadyLabel").visible = pReadyLabel
	
# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass
